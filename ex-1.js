// Declare ฟังก์ชัน add
function add(a, b) {
  return a + b;
}
console.log(add(10, 5));

// Declare ฟังก์ชัน subtract
function subtract(a, b) {
  return a - b;
}
console.log(subtract(10, 5));

// Declare ฟังก์ชัน multiply
function multiply(a, b) {
  return a * b;
}
console.log(multiply(10, 5));

// Declare ฟังก์ชัน divide
function divide(a, b) {
  return a / b;
}
console.log(divide(10, 5));
